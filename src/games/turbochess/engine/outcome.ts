import type { Side } from "./pieces";

/**
 * Чем кончилась партия.
 *
 * Отдельно от правил, потому что причину знают все: движок её ставит, комната
 * рассылает, экран показывает человеческим текстом. Набор причин — тот же,
 * что у шахмат; свои причины режимов — «все фигуры сняты», «сброшена бомба»,
 * «король дошёл до трона» — добавятся на их этапах (docs/MODES.md).
 */

/** Кто выиграл: номер стороны или ничья. */
export type Result = Side | "draw";

/**
 * Почему партия кончилась.
 *
 * Троекратное повторение и пятьдесят ходов по правилам — право заявить, а не
 * автомат; пятикратное и семьдесят пять — автомат (docs/SPEC.md, раздел 4).
 */
export type EndReason =
  | "checkmate"
  | "stalemate"
  | "resign"
  | "flag"
  | "insufficient"
  /** Флаг у одного, но у другого нечем матовать: по правилам — ничья. */
  | "flagVsInsufficient"
  | "threefold"
  | "fivefold"
  | "fiftyMoves"
  | "seventyFiveMoves"
  | "agreement"
  | "abandoned"
  /** Разошлись до первого хода: партии как будто и не было. */
  | "aborted"
  /** Предохранитель: слишком много ходов или слишком долго. */
  | "tooLong";

export interface Outcome {
  result: Result;
  reason: EndReason;
}

/** Человеческий текст причины. Показывается всем за столом и зрителям. */
export const REASON_TEXT: Record<EndReason, string> = {
  checkmate: "мат",
  stalemate: "пат: ходить нечем, а шаха нет",
  resign: "сдача",
  flag: "время вышло",
  insufficient: "мата не поставить ни одному, ни другому",
  flagVsInsufficient: "время вышло, но матовать нечем",
  threefold: "троекратное повторение",
  fivefold: "пятикратное повторение",
  fiftyMoves: "пятьдесят ходов без взятий",
  seventyFiveMoves: "семьдесят пять ходов без взятий",
  agreement: "ничья по соглашению",
  abandoned: "партия брошена",
  aborted: "партия отменена",
  tooLong: "партия затянулась",
};
