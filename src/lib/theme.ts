/**
 * Цвет фона платформы — тот же `--color-surface`, что в `globals.css`.
 *
 * Живёт здесь, а не только в стилях, потому что его спрашивают метаданные:
 * манифест приложения и мета-тег цвета шапки. Прочитать переменную CSS на
 * сервере нечем, поэтому значение приходится держать вторым экземпляром.
 *
 * Расходиться этим двум экземплярам не даёт `src/lib/theme.test.ts`: он
 * разбирает сам `globals.css` и сверяет. Цвет темы игры — в её манифесте, по
 * тому же правилу и с той же проверкой.
 */
export const PLATFORM_SURFACE = "#453466";
