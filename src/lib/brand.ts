/**
 * Имя платформы. Оно попадает в письма, в заголовок вкладки и в экран ошибки.
 *
 * Лежит в lib, а не в компоненте: имя нужно и почте, и метаданным, а тащить
 * ради строки React-компонент в почтовый слой незачем.
 *
 * Имя игры — не здесь. У каждой игры оно своё, в её манифесте.
 */
export const PLATFORM = "Кутёж";
