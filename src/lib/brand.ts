/**
 * Название, которое видит человек. В интерфейсе оно всегда русское —
 * «Платитутка»; латинское Pricetitute остаётся только именем репозитория.
 *
 * Лежит в lib, а не в компоненте: имя нужно и почте, и заголовку вкладки, а
 * тащить ради строки React-компонент в почтовый слой незачем.
 *
 * Это имя игры. Имени у платформы пока нет — оно появится вместе с текстами
 * (docs/BACKLOG.md B4).
 */
export const BRAND = "Платитутка";
