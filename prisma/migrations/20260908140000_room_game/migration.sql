-- Комната узнаёт, во что в ней играют (docs/BACKLOG.md A4).
--
-- Совместимая: колонка с умолчанием, старые строки заполняются сами. До
-- появления платформы игра была одна, поэтому умолчание — платитутка.
--
-- Общего зала это не касается: строки в базе у него нет вовсе, и свой ключ
-- каждая игра объявляет манифестом.
ALTER TABLE "platform"."private_rooms"
  ADD COLUMN "gameId" TEXT NOT NULL DEFAULT 'pricetitute';
